#
# TCEFORM
#
TCEFORM {
    tt_content {
        layout {
            altLabels {
                0 = normal
                1 = Color 1
                2 = Color 2
                3 = xyz
            }
            addItems {
                #4 = highlighted
                #                5 = hellgraue Box
            }
            removeItems = 3,4
        }
        # change default headline layout
        header_layout {
            removeItems = 4,5
            addItems {

            }
            altLabels {
                100 = (versteckt)
                0 = H1
                1 = Standard H2
                2 = Standard H3
                3 = Standard H4
            }
        }
        # remove not needed image positions for frontend
        imageorient {
            types.textmedia.removeItems = 1,2,9,10,17,18
            types.textpic.removeItems = 0,1,2,9,10,17,18,25
            types.image.removeItems = 1,2,8,9,10,17,18,25,26
            #            types.textmedia.altLabels {
            #                0 = Above, fullscreen
            #            }
        }
        # remove not needed image coloumns for frontend, 2 cols are enough
        imagecols.removeItems = 2,3,4,5,6,7,8

        # disable rarely used fields
        header_position.disabled = 0
        date.disabled = 1
        linkToTop.disabled = 1
        fe_group.disabled = 1
        spaceBefore.disabled = 1
        spaceAfter.disabled = 1
        section_frame.disabled = 1
        imageborder.disabled = 1
        image_compression.disabled = 1
        image_effects.disabled = 1
        image_noRows.disabled = 1
        imagecaption_position.disabled = 1
        rte_enabled.disabled = 1
        longdescURL.disabled = 1
    }
}
